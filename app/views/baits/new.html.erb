<% provide(:title, 'Bait regist') %>
<% provide(:class_text, 'エサ') %>
<% provide(:button_text, '新規登録') %>
<h1>エサ新規登録</h1>

<div class="row">
  <div class="col-md-10 col-md-offset-2">
    <%= form_with(model: @bait, url: user_baits_path(current_user), method: :post, local: true) do |f| %>
      <%= render 'shared/model_error_messages', model: @bait %>   
      <table class="table table-bordered">
        <tbody class="bulk-registration-form">
        <tr>
          <th>エサ</th>
        </tr>
          <div class="field">
            <td><%= f.text_field :bait_name, placeholder: "例.青虫", class: "form-control" %></td>
        </tr>
        </tbody>
      </table>
      <div class="center">
        <%= f.submit yield(:button_text), class: "btn btn-primary btn-#{yield(:button_text)}" %>
        <%= link_to "戻る", user_baits_path(current_user), class: "btn btn-default" %>
      </div>
    <% end %>
  </div>
</div>