<% provide(:title, 'Quetion') %>
<% provide(:class_text, 'question') %>
<% provide(:button_text, 'お問い合わせ送信') %>
<h1>お問い合わせフォーム</h1>

<div class="row">
  <div class="col-md-10 col-md-offset-2">
    <%= form_with(model: @contact, url: user_contacts_path(current_user), local: true) do |f| %>
      <%= render 'shared/model_error_messages', model: @contact %>

      <%= f.label :question_title, class: "label-#{yield(:class_text)}" %>
      <span class="text-danger"> (必須)</span>
      <%= f.text_field :question_title, placeholder: "お問い合わせの概要等を記載してください。", class: "form-control" %>

      <%= f.label :question_detail, class: "label-#{yield(:class_text)}" %>
      <span class="text-danger"> (必須)</span>
      <%= f.text_area :question_detail, placeholder: "詳細内容を記載してください。", class: "form-control", size: "40x10" %>

      <div class="button">
        <%= f.submit yield(:button_text), class: "btn btn-primary btn-#{yield(:button_text)}" %>
        <%= link_to "戻る", user_path(current_user), class: "btn btn-default" %>
      </div>
    <% end %>
  </div>
</div>